# 🚀 Деплой Lottery контракта в Mainnet

## Подготовка

1. **Создайте файл `.env` в папке `contracts/`:**
```env
WALLET_MNEMONIC=word1 word2 word3 ... word24
NETWORK=mainnet
TICKET_PRICE=1
TONCENTER_API_KEY=your_api_key  # опционально
```

2. **Убедитесь, что у вас установлены зависимости:**
```bash
cd contracts
npm install
```

## Запуск деплоя

### Быстрый деплой (автоматический скрипт):
```bash
npx tsx scripts/fix_and_deploy.ts
```

### Или через Blueprint (интерактивный):
```bash
npx blueprint run deployLottery
# Выберите mainnet в меню
```

## Что произойдет:

1. ✅ Автоматическая компиляция контракта
2. ✅ Проверка баланса кошелька
3. ✅ Деплой контракта в mainnet
4. ✅ Ожидание активации контракта
5. ✅ Вывод адреса контракта для проверки

## Важно:

- ⚠️ **Минимум 0.1 TON** на кошельке для деплоя
- ⚠️ **24 слова мнемоники** должны быть разделены пробелами
- ⚠️ **Никогда не коммитьте `.env` файл в git!**

## Проверка деплоя:

После успешного деплоя проверьте контракт:
- **Mainnet:** https://tonscan.org/address/YOUR_CONTRACT_ADDRESS
- **Testnet:** https://testnet.tonscan.org/address/YOUR_CONTRACT_ADDRESS

## Взаимодействие с контрактом:

### Покупка билета:
```typescript
await lottery.sendBuy(sender, toNano('1'));
```

### Розыгрыш (только владелец):
```typescript
await lottery.sendDraw(ownerSender, toNano('0.05'));
```

