#!/bin/bash

# ─────────────────────────────────────────────
# TONIX CHAIN — Build Script
# ─────────────────────────────────────────────

set -e

echo "🧠 Building Tonix Chain contracts..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

cd "$(dirname "$0")/.." || exit

# Проверка наличия необходимых инструментов
if ! command -v node &> /dev/null; then
    echo "❌ Node.js не найден. Установите Node.js."
    exit 1
fi

# Переход в папку contracts
cd contracts || exit

echo "📦 Компиляция контрактов через Blueprint..."
echo ""

# Компиляция всех контрактов
echo "1️⃣ Компиляция LotteryCore v2..."
npx tsx lotterycore-v2/compile.ts || {
    echo "❌ Ошибка компиляции LotteryCore v2"
    exit 1
}

echo ""
echo "2️⃣ Компиляция Vault..."
npx tsx -e "
import { compile } from '@ton/blueprint';
(async () => {
    try {
        await compile('Vault');
        console.log('✅ Vault скомпилирован');
    } catch (err: any) {
        console.error('❌ Ошибка:', err.message || err);
        process.exit(1);
    }
})();
" || {
    echo "❌ Ошибка компиляции Vault"
    exit 1
}

echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "✅ Build complete!"
echo "📁 Output directories:"
echo "   - lotterycore-v2/build/"
echo "   - vault/build/"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

