# 🔍 Отчёт: Настройка Vercel для Telegram Mini App

## 📊 Текущая ситуация

### ✅ Хорошие новости:
- **Домен `https://tonix-lottery-ui.vercel.app/` уже доступен** (HTTP 200)
- На домене развёрнуто приложение "TONIX Lottery — testnet"
- Домен работает и готов к использованию

### ⚠️ Текущая конфигурация:
- **Текущий проект Vercel:** `frontend`
- **Project ID:** `prj_Ou1D8XUFvHjQrnY4mYgPWaAKwQE0`
- **Текущий URL проекта:** `https://frontend-3t37u7nl6-danara1204s-projects.vercel.app`

### 🎯 Цель:
- Использовать домен: `https://tonix-lottery-ui.vercel.app/` для Telegram Mini App

---

## 🔧 Решение

### Вариант 1: Переименование проекта (Рекомендуется)

Если проект `tonix-lottery-ui` уже существует в Vercel, но это другой проект:

1. **Связать текущий `frontend` с проектом `tonix-lottery-ui`:**
   ```bash
   cd ~/tonix-chain/frontend
   vercel link --project=tonix-lottery-ui
   ```

2. **Задеплоить обновлённый код:**
   ```bash
   vercel --prod
   ```

### Вариант 2: Через Vercel Dashboard

1. Откройте: https://vercel.com/dashboard
2. Найдите проект `frontend`
3. Settings → General → Project Name
4. Переименуйте в: `tonix-lottery-ui`
5. Или найдите существующий проект `tonix-lottery-ui` и добавьте туда деплой

### Вариант 3: Создание нового проекта

Если нужно создать новый проект:

```bash
cd ~/tonix-chain/frontend
rm -rf .vercel
vercel link --project=tonix-lottery-ui
vercel --prod
```

---

## ✅ Итоговый Production URL для BotFather

```
https://tonix-lottery-ui.vercel.app/
```

**Или без слеша (оба варианта работают):**
```
https://tonix-lottery-ui.vercel.app
```

---

## 📋 Настройка в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Выберите бота: `@tonixchain_lottery_bot`
3. Bot Settings → Menu Button → Edit Menu Button
4. Вставьте URL: `https://tonix-lottery-ui.vercel.app/`
5. Сохраните

После настройки Mini App будет доступен по ссылке:
```
https://t.me/tonixchain_lottery_bot/app
```

---

## 🔍 Проверка после настройки

1. **Проверьте доступность:**
   ```bash
   curl -I https://tonix-lottery-ui.vercel.app/
   ```
   Должен вернуть: `HTTP/2 200`

2. **Проверьте в браузере:**
   - Откройте: https://tonix-lottery-ui.vercel.app/
   - Убедитесь что страница загружается без ошибок

3. **Проверьте в Telegram:**
   - Откройте бота: https://t.me/tonixchain_lottery_bot/app
   - Убедитесь что Mini App открывается

---

## ⚠️ Важные замечания

1. **Если домен уже работает:**
   - Убедитесь что вы деплоите правильный код в правильный проект
   - Проверьте что на домене развёрнута актуальная версия из `frontend/`

2. **Если получаете 404:**
   - Проверьте что проект переименован правильно
   - Убедитесь что деплой прошёл успешно: `vercel --prod`
   - Проверьте логи: `vercel logs --since=1h`

3. **Структура Next.js:**
   - Главная страница: `src/pages/index.tsx` → маршрут `/`
   - Это правильная точка входа для Mini App

---

## 🚀 Быстрая команда для деплоя

```bash
cd ~/tonix-chain/frontend
vercel link --project=tonix-lottery-ui  # Если нужно переименовать
vercel --prod                           # Деплой в production
```

---

**Дата проверки:** $(date)
**Статус домена:** ✅ Работает (HTTP 200)

