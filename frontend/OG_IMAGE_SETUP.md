# ✅ Настройка OG изображения для Next.js/Vercel

## 📋 Проверка настройки

Все проверки пройдены успешно:

### ✅ Файл
- **Путь:** `public/og-image.png`
- **Размер файла:** 54KB
- **Размер изображения:** 1200×630 пикселей (стандарт OG)
- **Формат:** PNG, RGB

### ✅ Мета-теги
- **Open Graph:** `og:image` → `https://tonix-lottery-ui.vercel.app/og-image.png`
- **Twitter:** `twitter:image` → `https://tonix-lottery-ui.vercel.app/og-image.png`
- **SITE_URL:** `https://tonix-lottery-ui.vercel.app`

### ✅ Next.js конфигурация
- Файлы из `public/` автоматически доступны по корневому пути `/`
- В `next.config.js` добавлены заголовки для кэширования изображений
- Режим `standalone` не влияет на обслуживание статики

---

## 🌐 URL для проверки

### Production (Vercel):
```
https://tonix-lottery-ui.vercel.app/og-image.png
```

### Локальная разработка:
```
http://localhost:3000/og-image.png
```

---

## 📱 Использование в Telegram

### Вариант 1: Автоматическое превью (рекомендуется)
1. Вставьте ссылку на сайт: `https://tonix-lottery-ui.vercel.app/`
2. Telegram автоматически подхватит `og:image` из мета-тегов
3. Превью появится автоматически

### Вариант 2: Прямой URL изображения
1. Вставьте прямой URL: `https://tonix-lottery-ui.vercel.app/og-image.png`
2. Telegram покажет изображение напрямую

### Вариант 3: В BotFather (Menu Button)
1. URL для Mini App: `https://tonix-lottery-ui.vercel.app/`
2. Telegram использует `og:image` для превью

---

## 🔍 Проверка после деплоя

### Проверка доступности файла:
```bash
curl -I https://tonix-lottery-ui.vercel.app/og-image.png
```

Ожидаемый ответ:
```
HTTP/2 200
Content-Type: image/png
Cache-Control: public, max-age=31536000, immutable
```

### Проверка мета-тегов:
```bash
curl https://tonix-lottery-ui.vercel.app/ | grep -i "og:image"
```

Должен вернуть:
```html
<meta property="og:image" content="https://tonix-lottery-ui.vercel.app/og-image.png" />
```

### Проверка через онлайн-инструменты:
- **Facebook Debugger:** https://developers.facebook.com/tools/debug/
- **Twitter Card Validator:** https://cards-dev.twitter.com/validator
- **Telegram:** Просто вставьте ссылку в чат

---

## 🛠 Решение проблем

### Если изображение не отображается:

1. **Проверьте путь:**
   - Файл должен быть в `public/og-image.png`
   - Не в `public/images/og-image.png` или других подпапках

2. **Проверьте мета-теги:**
   - Убедитесь, что `og:image` указан с абсолютным URL
   - Пример: `https://tonix-lottery-ui.vercel.app/og-image.png`
   - Не используйте относительные пути: `/og-image.png` (хотя они тоже работают)

3. **Проверьте деплой:**
   - Убедитесь, что файл загружен в Vercel
   - Проверьте логи деплоя: `vercel logs`

4. **Очистите кэш:**
   - Telegram может кэшировать превью
   - Попробуйте добавить версию: `/og-image.png?v=2`

---

## 📝 Структура файлов

```
frontend/
├── public/
│   ├── og-image.png          ✅ 1200×630, 54KB
│   ├── telegram-cover.png    ✅ 1280×720, 59KB
│   └── ...
├── src/
│   └── pages/
│       └── index.tsx         ✅ Мета-теги с og:image
└── next.config.js            ✅ Заголовки для кэширования
```

---

## ✅ Итоговая проверка

- [x] Файл `public/og-image.png` существует
- [x] Размер изображения 1200×630
- [x] Мета-теги `og:image` настроены
- [x] URL абсолютный и правильный
- [x] `next.config.js` настроен
- [x] Готово к деплою на Vercel

**После деплоя изображение будет доступно по:**
```
https://tonix-lottery-ui.vercel.app/og-image.png
```

**Telegram автоматически подхватит превью при шаринге ссылки:**
```
https://tonix-lottery-ui.vercel.app/
```

