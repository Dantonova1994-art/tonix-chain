# 📹 Создание видео/GIF из анимации кнопки

Для создания видео или GIF из HTML-анимации кнопки можно использовать несколько методов:

## Метод 1: Использование Puppeteer + FFmpeg (рекомендуется)

### Установка зависимостей:

```bash
npm install puppeteer
# или
brew install ffmpeg
```

### Скрипт для генерации:

```javascript
const puppeteer = require('puppeteer');
const { exec } = require('child_process');

(async () => {
  const browser = await puppeteer.launch();
  const page = await browser.newPage();
  
  await page.setViewport({ width: 400, height: 200 });
  await page.goto('file://' + __dirname + '/public/start-button-animation.html');
  
  // Запись экрана
  await page.screenshot({ path: 'button-frame.png' });
  
  await browser.close();
})();
```

## Метод 2: Использование онлайн-сервисов

1. **ScreenToGif** (Windows) или **LICEcap** (Mac/Windows)
   - Откройте `public/start-button-animation.html` в браузере
   - Запишите экран с помощью инструмента
   - Экспортируйте в MP4 или GIF

2. **CloudConvert API** или **EZGIF.com**
   - Загрузите скриншоты
   - Конвертируйте в GIF/MP4

## Метод 3: Использование CSS Animation → Video

Можно использовать инструменты вроде:
- **remotion** (React-based video generation)
- **Lottie** (если конвертировать в Lottie JSON)

## Рекомендуемый размер для Telegram-постов:

- **GIF:** 320×240 или 640×480, до 10MB
- **MP4:** 1280×720, до 50MB

## Быстрая команда (если установлен FFmpeg):

```bash
# Запись экрана с браузера (Mac)
ffmpeg -f avfoundation -r 30 -i "1:0" -t 3 output.mp4

# Или используйте OBS Studio для записи экрана
```

---

**💡 Совет:** Для Telegram-постов лучше использовать GIF (до 10MB) или короткое MP4 (3-5 секунд).

